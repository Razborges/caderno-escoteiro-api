type Group {
  id: ID!
  number: Int!
  state: String
  user: [User]!
}

input GroupInput {
  number: Int!
  state: String!
}

input GroupUpdate {
  number: Int
  state: String
}

input GroupFilter {
  number: Int
  state: String
}
